langcode: en
status: true
dependencies:
  module:
    - menu_ui
third_party_settings:
  menu_ui:
    available_menus:
      - main
    parent: 'main:'
name: 'General Page'
type: dl_general_page
description: "The General Page content type is the standard, generic page. It has fields for title, body, media, Folwell components, and tags. The default layout is two column and the left column is wider than the right. The media, body, and tags are in the left column, and Folwell components in the right column. The layout can be changed, and additional content blocks can be added to the page.\r\n"
help: ''
new_revision: true
preview_mode: 1
display_submitted: true
